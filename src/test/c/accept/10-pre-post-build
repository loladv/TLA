project build_automation
compiler clang
flags { -Wall }
sources { main.c }
pre_build {
rm -rf build/
mkdir build
}
post_build {
cp output_bin /usr/local/bin/
echo Build_completado > log.txt
}

build
run


